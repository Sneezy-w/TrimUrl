<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <a class="navbar-brand" href="/">TrimURL</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item" id="loginNav">
                    <button class="nav-link btn btn-link {{#if logged_in.status}}d-none{{/if}}" onclick="signInWithGoogle()">
                        <i class="bi bi-google me-2"></i>Sign in with Google
                    </button>
                </li>
                <li class="nav-item {{#if logged_in.status}}{{else}}d-none{{/if}}" id="logoutNav">
                    <a class="nav-link" href="/auth/sessionLogout">Logout</a>
                </li>
            </ul>
        </div>
    </div>
    
</nav> 